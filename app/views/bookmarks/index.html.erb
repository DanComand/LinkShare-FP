<h1>New Bookmark</h1>

<%= render 'form' %>

<% @bookmarks.each do |bookmark| %>


<div class="bookmark-content">
	

	<p><a href="<%= bookmark.url %>" target="_blank"><%= bookmark.title %></a></p>
	<p>Posted by: <%= bookmark.user.firstName %><img src="<%= bookmark.user.avatar.thumb %>" ></p>

	
	<a href="<%= bookmark.url %>" target="_blank"><img src="<%= bookmark.image %>", width="300" height="100%"></a><br>
	<p><%= bookmark.description %></p> 

	<%= form_for([bookmark, Note.new]) do |f| %>

		<div class="field">
			<%= f.label :comment %><br /> 
			<%= f.text_area :comment %> 
		</div>
		<div class="actions"> 
			<%= f.submit %> 
		</div>

	<% end %>

	<h3>Comments</h3>
	<p>Comments for: <%= bookmark.title %></p>
	<% bookmark.notes.each do |note| %>
	<p>
		<%= note.comment %>
		<br>

		<% if note.user != nil %>
		<em> by <%= note.user.firstName %></em>
		Added on: <%= note.created_at %>
		<% end %>
	</p>
	<% end %>


	<%= link_to "Delete", bookmark, method: :delete, data: {confirm: "Are you sure you want to delete this bookmark?"}  %>
</div>
<% end %>



